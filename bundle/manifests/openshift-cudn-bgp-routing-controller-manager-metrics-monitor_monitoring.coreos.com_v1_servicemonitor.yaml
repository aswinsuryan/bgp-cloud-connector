apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: openshift-cudn-bgp-routing
    control-plane: controller-manager
  name: openshift-cudn-bgp-routing-controller-manager-metrics-monitor
spec:
  endpoints:
  - bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    path: /metrics
    port: https
    scheme: https
    tlsConfig:
      caFile: /etc/prometheus/configmaps/serving-certs-ca-bundle/service-ca.crt
      serverName: openshift-cudn-bgp-routing-controller-manager-metrics-service.openshift-cudn-bgp-routing.svc
  namespaceSelector:
    matchNames:
    - openshift-cudn-bgp-routing
  selector:
    matchLabels:
      app.kubernetes.io/name: openshift-cudn-bgp-routing
      control-plane: controller-manager
